# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — ignora tudo, exceto o template
.env*
!.env.example

# playwright
playwright/.auth/
test-results/
playwright-report/

# k6 load test reports (HTMLs gerados — ficam locais, não vão pro repo)
tests/load/reports/*.html
tests/load/reports/*.json

# claude code (só settings são específicos por máquina — commands/skills vão pro git)
.claude/settings.json
.claude/settings.local.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
